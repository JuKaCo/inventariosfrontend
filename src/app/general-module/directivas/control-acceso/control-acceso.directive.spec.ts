import { ControlAccesoDirective } from './control-acceso.directive';

describe('ControlAccesoDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlAccesoDirective();
    expect(directive).toBeTruthy();
  });
});
